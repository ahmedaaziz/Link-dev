import { ConvertObjPipe } from './convert-obj.pipe';

describe('ConvertObjPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertObjPipe();
    expect(pipe).toBeTruthy();
  });
});
